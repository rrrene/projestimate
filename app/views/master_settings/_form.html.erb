<%= form_for(@master_setting) do |f| %>
  <% if @master_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@master_setting.errors.count, "error") %> prohibited this master_setting from being saved:</h2>

      <ul>
      <% @master_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>

    <%= f.label :value %><br />
    <%= f.text_area :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
