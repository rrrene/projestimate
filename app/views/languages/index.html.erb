<div class="span12">
  <h1>Languages</h1>

  <table class="table table-striped">
    <tr>
      <th>Name</th>
      <th>Locale</th>
      <th></th>
      <th></th>
    </tr>

  <% @languages.each do |language| %>
    <tr >
      <td><%= language.name %></td>
      <td><%= language.locale %></td>
      <td><%= link_to 'Edit', edit_language_path(language), :class => "icn_edit" %></td>
      <td><%= link_to 'Destroy', language, confirm: 'Are you sure?', method: :delete, :class => "icn_delete" %></td>
    </tr>
  <% end %>
  </table>

  <br />

  <%= link_to 'New Language', new_language_path, :class => "btn" %>
</div>