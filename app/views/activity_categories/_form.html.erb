<%= form_for(@activity_category) do |f| %>
  <% if @activity_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_category.errors.count, "error") %> prohibited this activity_category from being saved:</h2>

      <ul>
      <% @activity_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>

    <%= f.label :name %><br />
    <%= f.text_field :name %>

    <%= f.label :description %><br />
    <%= f.text_area :description %>

    <%= f.label :alias %><br />
    <%= f.text_field :alias %>

    <%= f.label :project_area_id %><br />
    <%= f.number_field :project_area_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
