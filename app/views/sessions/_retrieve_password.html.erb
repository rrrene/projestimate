<%= simple_form_for @user, :url => password_reset_path(params[:id]) do |f| %>
    <%= f.input :password %>
    <%= f.input :password_confirmation %>
    <div class="actions">
      <%= f.submit "Update your password" %>
    </div>
<% end %>