<%= form_for(@help_type) do |f| %>
  <% if @help_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@help_type.errors.count, "error") %> prohibited this help_type from being saved:</h2>

      <ul>
      <% @help_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div >
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
