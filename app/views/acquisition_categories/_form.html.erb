<div class="span12">
  <%= form_for(@acquisition_category) do |f| %>
    <% if @acquisition_category.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@acquisition_category.errors.count, "error") %> prohibited this acquisition_category from being saved:</h2>

        <ul>
        <% @acquisition_category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>

      <%= f.label :description %><br />
      <%= f.text_area :description %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>
