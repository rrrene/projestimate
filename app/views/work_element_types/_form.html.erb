<%= form_for(@work_element_type) do |f| %>
  <% if @work_element_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_element_type.errors.count, "error") %> prohibited this work_element_type from being saved:</h2>

      <ul>
      <% @work_element_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div >
    <%= f.label :name %><br />
    <%= f.text_field :name %>

    <%= f.label :alias %><br />
    <%= f.text_field :alias %>

    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
