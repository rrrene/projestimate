<div class="span12">
  <%= form_for(@project_security_level) do |f| %>
    <% if @project_security_level.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@project_security_level.errors.count, "error") %> prohibited this project_security_level from being saved:</h2>

        <ul>
        <% @project_security_level.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.label :name %><br />
    <%= f.text_field :name %>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>
