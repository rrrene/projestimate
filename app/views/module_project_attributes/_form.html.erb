<div class="span12">
  <%= simple_form_for(@mpa) do |f| %>

    <%= f.input :in_out, :label => "#{I18n.t "in_out"}" %>

    <div class="actions">
      <%= f.submit "#{I18n.t "save"}", :class => "btn" %>
      <%= f.submit "#{I18n.t "apply"}", :class => "btn" %>
    </div>
  <% end %>
</div>
